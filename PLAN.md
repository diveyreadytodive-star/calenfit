# 캘린더 기반 기회·지원 에이전트 — 금융 AI Challenge MVP 계획

## 결정

반응형 **웹서비스**로 개발한다. 대회는 실행 가능한 웹서비스 URL을 요구하므로, 네이티브 앱만으로는 제출 요건을 안전하게 충족하지 못한다. 모바일 앱처럼 보이고 설치 가능한 PWA는 후속 선택지이지만, 심사자는 로그인 없이 URL에서 데모를 재현할 수 있어야 한다.

## 제품 한 줄

**캘린더를 잘 쓰면, 돈이 나온다.**

면접·시험·창업 등 일정에서 지원 가능 사건을 감지하고, 증빙을 제때 확보하게 하며, 공식 공고의 접수·마감·예산 상태를 추적해 신청 완료까지 돕는다.

## 제출용 얇은 범위

- 사용자: 경기도 거주 미취업 청년
- 이벤트: 취업 면접, 어학·자격시험
- 정책: 경기도 청년 면접수당, 경기청년 역량강화 기회지원
- 핵심 시나리오: 면접 일정 → 면접확인서 요청/대체 증빙 복구 → 공식 접수 상태 → 신청 체크리스트
- 데모 방식: 사전 구성한 가상 사용자와 이벤트 2개. 실제 공고 URL·확인 시각은 노출하되, 실제 신청·마이데이터·메일 발송은 하지 않는다.

## 성공 기준

1. 로그인 없이 공개 URL에서 데모 계정/데모 데이터로 첫 화면을 열 수 있다.
2. 면접·시험 이벤트를 생성 또는 ICS 파일로 불러오면 10초 안에 이벤트 유형과 연결된 정책 후보가 표시된다.
3. 각 정책 후보는 `가능성 높음`, `확인 필요`, `현재 대상 아님` 중 하나와 그 이유를 표시한다. 최종 수급 가능이라고 단정하지 않는다.
4. 면접 이벤트에는 D-3, D+1, D+3, 접수 시작, 마감 임박 태스크가 생성된다.
5. 사용자가 면접확인서 없음 상태를 선택하면 대체 증빙 목록·부족한 증빙·공식 안내 링크가 표시된다.
6. 이미지/PDF 증빙 파일 하나 이상을 올리면 문서 유형과 회사명/날짜/금액/시험명 중 인식 가능한 항목이 화면에 표시된다.
7. 각 정책은 공식 출처 URL, 마지막 확인 시각, 접수 상태를 표시한다.
8. 모바일 390px 폭과 데스크톱에서 주요 흐름을 완료할 수 있다.
9. 시크릿 창에서 데모 URL을 열고, API 키·개인 계정·OAuth 설정 없이 전체 데모가 작동한다.

## 아키텍처

```text
Responsive Web UI
  ├─ Calendar/Event inbox
  ├─ Opportunity dashboard
  ├─ Evidence vault
  └─ Recovery workflow
        ↓
Application API
  ├─ Event classifier (AI)
  ├─ Policy rules engine (deterministic)
  ├─ Evidence extractor (OCR/vision AI)
  ├─ Policy status adapter
  └─ Notification/task scheduler
        ↓
Persistence
  ├─ users / profiles
  ├─ events / policy matches / tasks
  ├─ policy source snapshots
  └─ evidence metadata (demo files only)
```

### AI와 결정 규칙

| 영역 | 방식 | 제한 |
|---|---|---|
| 일정 분류 | LLM이 제목·설명에서 면접/시험/창업 후보 추출 | 사용자가 확인·수정 가능 |
| 정책 매칭 | 연령·거주지·취업 상태·이벤트를 규칙으로 평가 | AI가 최종 자격을 확정하지 않음 |
| 증빙 분석 | OCR/vision이 문서 필드와 유형 추출 | 원본·추출값을 함께 표시 |
| 복구 모드 | 보유 증빙을 대체 증빙 규칙에 매핑 | `대체 신청 경로`, `추가 확인 필요`만 표시 |
| 정책 상태 | 공식 공고에서 수집한 상태 + 확인 시각 | 파싱 실패 시 `확인 필요`, 추정 금지 |

## 데이터 모델

- `UserProfile`: 거주 시·군, 연령대, 취업 상태, 학교/학과, 관심 분야, 동의 범위
- `CalendarEvent`: 제목, 날짜, 원문 설명, AI 분류, 사용자 확인 상태
- `Policy`: 제공기관, 대상 조건, 이벤트 트리거, 증빙 요건, 공식 URL
- `PolicyStatus`: 접수 상태, 마감, 예산 소진 여부, source URL, checkedAt
- `PolicyMatch`: 이벤트-정책 연결, 가능성, 근거, 확인 질문
- `Evidence`: 파일 메타데이터, 문서 유형, OCR 추출값, 사건 연결
- `Task`: 시점, 행동, 완료 여부, 근거 정책
- `RecoveryCase`: 놓친 증빙, 보유 대체 증빙, 추가 행동

## 구현 순서

1. **프로젝트 골격과 데모 데이터**
   - 반응형 웹 프로젝트를 별도 앱 디렉터리에 초기화한다.
   - 가상 사용자 `경기도 거주·미취업·24세`와 면접/정보보안기사 시험 이벤트를 시드한다.
   - 정책 2개의 요건·증빙·공식 URL·상태를 버전 관리되는 JSON/DB 데이터로 등록한다.

2. **정책 규칙·상태 계층**
   - 프로필과 이벤트를 입력받아 정책 후보와 이유를 반환하는 결정론적 규칙을 구현한다.
   - 공식 공고 출처, 확인 시각, 접수/마감/예산소진/확인필요 상태를 표시한다.
   - 초기에는 상태 수집 실패 시 자동 판단하지 않고, 관리자/시드 데이터의 상태를 보수적으로 노출한다.

3. **일정 인입과 AI 분류**
   - 수동 이벤트 생성과 ICS 업로드를 제공한다.
   - Google Calendar OAuth는 데모 안정성을 해치므로 2차 기능으로 분리한다.
   - AI가 면접/시험/창업 후보를 분류하고, 사용자 확인 후에만 태스크를 만든다.

4. **증빙·복구 워크플로**
   - 이벤트별 증빙 체크리스트와 D-3/D+1/D+3/마감 태스크를 구현한다.
   - 파일 업로드와 OCR 추출을 추가한다.
   - `면접확인서 없음`의 경우, 대체서약서·면접 안내문·채용공고·결과 안내·명함·현장 사진의 보유 여부를 받아 부족 항목을 표시한다.

5. **대시보드와 행동 UX**
   - 예상 회수액, 다음 행동, 상태, 마감 카운트다운을 우선순위 순으로 배치한다.
   - 공식 신청 페이지 이동, 확인서 요청 이메일 초안 복사, 증빙 업로드를 한 사건 화면에서 끝낸다.
   - 알림은 우선 앱 내 태스크와 브라우저 알림으로 구현한다.

6. **제출물·배포·검증**
   - 공개 배포 URL, 데모 계정 또는 로그인 없는 데모 모드, 시드 데이터를 제공한다.
   - 기획서·기능명세서·README에는 AI의 입력/판단/안내/기록 흐름과 한계를 동일하게 적는다.
   - 시크릿 창·모바일·정책 상태 실패·문서 업로드 실패를 검증한다.

## 명시적 제외 범위

- 전국 모든 정책 및 자동 크롤링
- Gmail 전체 읽기와 자동 메일 발송
- Google Calendar OAuth 필수화
- 정부24/잡아바 자동 신청
- 마이데이터·지역화폐·실제 개인정보 연동
- 최종 수급 자격 또는 예산 소진의 AI 추정
- 금융상품 추천

## 공수

| 범위 | 1인 개발 예상 |
|---|---:|
| 제출용 얇은 MVP | 5~8일 |
| Google Calendar OAuth, 정책 10개, 알림 안정화 | 추가 3~5일 |
| 학교·단과대·지자체 커넥터와 정책 상태 수집 | 추가 2~4주 |
| 전국 정책·공공 마이데이터·신청 연동 | 제품 단계, 별도 협력 필요 |

## 위험과 완화

| 위험 | 완화 |
|---|---|
| 정책 조건·마감이 변경됨 | 공식 URL·checkedAt 노출, 불명 시 확인 필요로 표시 |
| AI가 자격을 과장 | 규칙 기반 가능성 표시, 최종 심사 고지 |
| 캘린더·문서의 개인정보 | 데모는 가상 데이터만, 실제 서비스는 최소 권한·삭제 기능·암호화 필요 |
| OAuth/외부 API로 데모가 실패 | 수동 이벤트·ICS·시드 데이터로 모든 흐름을 재현 가능하게 유지 |
| 범위가 넓어짐 | 제출 전에는 경기도 정책 2개와 이벤트 2개만 유지 |

## 검증 시나리오

1. `안랩 PM 면접`을 생성한다 → 면접수당 후보와 D+1 확인서 요청 태스크가 생성된다.
2. 면접확인서 없음으로 전환한다 → 보유한 초대 이메일·채용공고를 대체 증빙으로 표시하고 부족 항목을 안내한다.
3. `정보보안기사 필기`를 생성한다 → 응시료 지원 후보와 영수증 저장 태스크가 생성된다.
4. 정책 상태를 `예산 소진`으로 설정한다 → 신청 CTA가 비활성화되고 공식 공고 링크와 상태 확인 시각이 표시된다.
5. 모바일 브라우저·시크릿 창에서 위 시나리오를 처음부터 끝까지 재현한다.

## 근거

- `2609/2026-금융-AI-Challenge.md:20-27, 47-59` — 실제 웹서비스 URL과 접근 가능 기간이 제출 요건임.
- `2609/2026-금융-AI-Challenge.md:67-72` — 단일 사용자·문제·핵심 흐름, AI 한계와 출처를 명확히 해야 함.
- 경기도 청년 면접수당 공식 안내 — 면접확인서 또는 대체 증빙, 공식 신청 절차.
- 경기청년 역량강화 기회지원 공식 안내 — 시험 응시료 지원과 필수 증빙.

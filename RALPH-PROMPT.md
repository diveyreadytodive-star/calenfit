# Ralph 실행 프롬프트 — 금융 AI Challenge 1차 완성본

아래 전체를 새 Codex 세션에서 그대로 실행한다.

```text
$ralph --prd --no-deslop

2026 금융 AI Challenge 제출용 반응형 웹서비스의 1차 완성본을 끝까지 구현하고 검증하라. 중간 계획이나 부분 결과만 보고하고 멈추지 말고, 아래 완료 조건이 모두 충족될 때까지 반복해서 수정하라.

## 작업 위치와 기존 자료

- 작업 루트: `2609/2026-금융-AI-Challenge/`
- 제품/개발 계획: `2609/2026-금융-AI-Challenge/PLAN.md`
- 현재 코드: 같은 폴더의 `index.html`, `styles.css`, `app.js`
- 대회 정보: `2609/2026-금융-AI-Challenge.md`
- 현재 코드는 정적 초안과 별도 JS/CSS가 섞여 있으므로 먼저 전체를 읽고, 중복을 제거해 하나의 일관된 앱으로 통합하라.

## 절대 제약

- 제품명은 `캘린핏(Calenfit)`으로 확정됐다. 코드 구조는 브랜드 문자열에 종속되지 않게 유지하라.
- GitHub 저장소 `diveyreadytodive-star/calenfit`을 사용하고, 검증이 통과한 의미 단위마다 Lore 형식으로 커밋·푸시하라.
- 이 Obsidian Vault의 다른 폴더와 파일을 수정하지 마라. Ralph 런타임용 `.omx` 상태·PRD·테스트 명세와 위 작업 루트만 수정 가능하다.
- 사용자 실제 개인정보, 캘린더, 이메일, 문서를 수집하지 마라. 데모는 명백히 가상인 데이터만 쓴다.
- AI가 최종 수급 자격, 예산 소진, 정책 유효성을 추측하거나 확정하게 하지 마라.
- 공식 공고 URL, 마지막 확인 시각, 불확실성 표시를 UI에서 항상 노출하라.
- 대회 제출 요건상 네이티브 앱이 아니라 외부 URL로 접근 가능한 웹서비스여야 한다.

## 모델 라우팅과 토큰 효율

Ralph가 하위 에이전트를 위임할 때 모델을 반드시 명시하고 아래 기준을 지켜라. 동일 작업을 여러 모델에 중복 위임하지 말고, Sol이 내린 결정은 구현 요구사항으로 압축해 Luna에 전달하라.

### `gpt-5.6-sol` — 고지능 판단 전용

- reasoning effort: `high` 이상
- 사용 범위:
  - PRD·테스트 명세의 핵심 요구사항과 수용 기준 확정
  - 전체 아키텍처와 데이터/신뢰 경계 결정
  - 정책 규칙 엔진과 AI 역할 분리 검토
  - 캘린더·증빙 개인정보 및 금융 오판 위험 검토
  - 복잡한 통합 실패·회귀의 원인 분석
  - 최종 architect 검증과 완료 승인
- 단순 HTML/CSS 작성, 반복적인 컴포넌트 구현, 일반 테스트 작성에는 사용하지 마라.

### `gpt-5.6-luna` — 구현·반복 작업 전용

- reasoning effort: `medium` 기본, 디버깅 시 `high`
- 사용 범위:
  - HTML/CSS/JavaScript 구현과 파일 분리
  - 반응형 레이아웃·접근성 속성·상호작용 구현
  - 시드 데이터·정책 JSON·fixture 작성
  - localStorage, ICS 파서, 태스크 타임라인 구현
  - 단위 테스트·브라우저 테스트·문서 작성
  - Sol이 승인한 설계에 따른 버그 수정과 반복 개선
- 범위를 바꾸거나 보안/금융 판단 규칙을 새로 정해야 하면 임의 결정하지 말고 Sol 검토로 올려라.

### 권장 에이전트 배치

1. `planner/architect` 1명 — `gpt-5.6-sol`, PRD·아키텍처·경계 확정
2. `executor` 최대 2명 — `gpt-5.6-luna`, UI/상태 로직과 테스트를 충돌 없는 파일 단위로 나눠 구현
3. `test-engineer` 1명 — `gpt-5.6-luna`, 수용 기준 기반 검증
4. `architect/verifier` 1명 — `gpt-5.6-sol`, 구현 완료 후에만 최종 검증

독립 구현 작업만 병렬화하고, 설계 승인 → 구현 → 검증의 의존 단계는 순차 실행하라. Sol에게 전체 소스와 긴 로그를 반복해서 넘기지 말고, 변경 요약·인터페이스·실패 증거만 전달하라.

## Ralph 계획 게이트

구현 전에 반드시 다음 두 파일을 만들고 서로 일치하는지 확인하라.

- `.omx/plans/prd-calendar-benefit-agent.md`
- `.omx/plans/test-spec-calendar-benefit-agent.md`

PRD에는 아래 사용자 스토리와 범위를 넣고, 테스트 명세에는 각 완료 조건을 검증하는 단위/통합/E2E 시나리오를 넣어라. 계획이 끝나기 전에는 구현을 시작하지 마라.

## 제품 정의

슬로건: `캘린더를 잘 쓰면, 돈이 나온다.`

면접·시험 같은 일정을 읽고, 놓치기 쉬운 지원금 후보를 연결하며, 나중에 필요한 증빙을 적절한 시점에 확보하도록 행동을 만들어 주는 캘린더 기반 기회·지원 에이전트다.

첫 MVP의 타깃은 `경기도 거주 미취업 청년`이고 실제 지원 정책은 아래 두 개만 구현한다.

1. 경기도 청년 면접수당
2. 경기청년 역량강화 기회지원(어학·자격시험 응시료)

전국 정책, 학교/학과 정책, 창업 지원은 확장 가능 구조와 샘플 카드까지만 보여주고 실제 매칭 규칙에는 넣지 마라.

## 필수 사용자 시나리오

### 시나리오 A — 면접수당 예방 모드

1. 사용자가 `가상핀테크 PM 면접`, 날짜 `2026-09-05`, 대면 면접 이벤트를 추가한다.
2. 앱이 이를 면접 이벤트로 분류하고 경기도 청년 면접수당 후보를 보여준다.
3. D-3에는 채용공고·면접 안내 저장, D+1에는 면접확인서 요청, D+3에는 미회신 후속 태스크를 생성한다.
4. 사용자가 태스크를 완료/미완료 처리할 수 있고 새로고침 후에도 상태가 유지된다.

### 시나리오 B — 면접확인서 복구 모드

1. `면접확인서 없음`을 선택한다.
2. 면접 안내 이메일, 채용공고, 면접 결과 안내, 명함, 현장 사진 등 대체 증빙 체크리스트가 열린다.
3. 사용자가 보유한 증빙을 체크하면 현재 확보 상태와 부족한 다음 행동을 보여준다.
4. `대체 신청 경로 있음 / 추가 확인 필요 / 최종 판단은 운영기관`을 구분해 표시한다.
5. 확인서 요청 이메일 초안을 복사할 수 있다.

### 시나리오 C — 시험 응시료 지원

1. 사용자가 `정보보안기사 필기`, 날짜 `2026-10-02` 이벤트를 추가한다.
2. 앱이 시험 이벤트로 분류하고 응시료 지원 후보를 보여준다.
3. 결제영수증, 접수확인, 응시 사실, 응시일 취업 상태의 체크리스트를 생성한다.
4. 현재 접수 상태와 공식 신청 링크를 보여준다.

### 시나리오 D — 정책 상태 변경

1. 데모 컨트롤에서 정책 상태를 `접수 중`, `마감`, `예산 소진`, `확인 필요`로 바꿀 수 있다.
2. 마감/예산 소진 상태에서는 신청 CTA가 비활성화된다.
3. 모든 상태에 공식 출처와 마지막 확인 시각이 표시된다.

## 필수 기능

- 모바일 우선 반응형 대시보드와 데스크톱 레이아웃
- 로그인 없는 `데모 모드`
- 면접·시험 이벤트 수동 추가/수정/삭제
- `.ics` 파일 가져오기. 파싱 실패 시 사용자가 이해할 수 있는 오류 표시
- 일정 제목/설명 기반 이벤트 분류 모듈
  - API 키가 있으면 LLM 어댑터를 사용할 수 있는 인터페이스
  - 키가 없거나 API가 실패하면 결정론적 로컬 분류기로 데모가 계속 동작
  - UI에 `AI 분류` 또는 `로컬 안전 분류` 출처를 표시
- 정책 규칙 엔진: AI가 아니라 명시적 조건으로 자격 가능성을 평가
- 지원 가능성 상태: `가능성 높음`, `확인 필요`, `현재 대상 아님`
- 태스크 타임라인: D-3, D+1, D+3, 접수 시작, 마감 임박
- 증빙 보관함 UI와 문서 메타데이터 표시
  - 실제 파일을 외부로 전송하지 말 것
  - 브라우저 로컬 데모 범위에서 파일명, 유형, 크기, 연결 이벤트만 관리
- 대체 증빙 복구 모드
- 정책 상태, 공식 출처, 확인 시각, 공식 신청 링크
- localStorage 기반 데모 상태 지속 및 `데모 초기화` 버튼
- 접근성: 키보드 조작, 명확한 focus, label, aria-live, 색상 외 상태 표현
- 빈 상태, 파싱 실패, AI 실패, 정책 상태 미확인 오류 화면

## 기술 방향

- 현재 정적 앱을 우선 정상화한다. 프레임워크 마이그레이션은 완료 조건 달성에 명확히 필요한 경우에만 허용한다.
- 불필요한 신규 의존성을 추가하지 마라.
- HTML/CSS/JavaScript만으로 완료 가능하면 유지하고, 모듈을 기능별로 분리하라.
- 시드 데이터와 정책 규칙은 UI 코드와 분리된 명확한 구조로 둔다.
- 공고 상태 크롤링을 실제로 구현하지 말고, `PolicySourceAdapter` 인터페이스와 검증된 데모 스냅샷으로 구현한다. 라이브 크롤링은 후속 범위다.
- 실제 알림 서버는 구현하지 않고 앱 내 알림센터와 브라우저 Notification API를 선택적으로 제공한다. 권한 거부 시 앱이 정상 동작해야 한다.

## 화면 요구사항

1. 랜딩/대시보드: 예상 회수 가능 금액, 오늘 할 일, 다가오는 일정, 정책 상태
2. 일정 인박스: AI 분류 결과와 사용자 확인/수정
3. 사건 상세: 연결 정책, 태스크 타임라인, 증빙 체크리스트
4. 복구 모드: 놓친 증빙의 대안과 부족한 행동
5. 정책 상세: 자격 조건, 모집 상태, 공식 출처, 확인 시각
6. 데모 컨트롤: 정책 상태 전환, 데이터 초기화

디자인은 신뢰감 있는 금융 서비스 톤으로 하고, 현재 초안의 따뜻한 배경과 카드 스타일은 유지하되 정보 위계를 정리하라. 모바일 390×844와 데스크톱 1440×900에서 가독성을 확인하라. 참고 이미지가 없으므로 픽셀 매칭용 visual-verdict는 쓰지 말고 실제 브라우저 스크린샷을 직접 검사하라.

## 완료 조건

- 공개 정적 서버에서 모든 필수 시나리오를 재현할 수 있다.
- 앱 로드 시 콘솔 오류가 0개다.
- 모든 버튼과 링크가 동작하거나 명확히 비활성 상태를 갖는다. `alert()` 임시 구현과 죽은 버튼이 남아 있지 않다.
- 새로고침 후 이벤트·태스크·증빙 체크 상태가 유지된다.
- 모바일/데스크톱 스크린샷을 생성해 깨짐을 확인한다.
- HTML 검증, JavaScript 문법 검사, 링크/자산 경로 검사, 핵심 로직 테스트가 통과한다.
- 테스트 결과와 남은 비목표 범위를 `README.md`에 기록한다.
- 기획서/기능명세서 작성에 바로 쓸 수 있도록 `docs/FEATURE-SPEC.md`에 입력→판단→안내→기록 흐름, AI 사용 지점, 안전 한계를 정리한다.
- architect 역할의 최종 검증에서 `APPROVED`를 받아야 한다.
- 완료 시 변경 파일, 실행 방법, 테스트 결과, 알려진 후속 범위를 보고하고 아래 문구를 마지막 줄에 정확히 출력한다.

<promise>CALENDAR BENEFIT MVP V1 COMPLETE</promise>
```

## Goal 모드 대안

Ralph 대신 장기 Goal로 실행하려면 아래 objective를 사용한다.

```text
`2609/2026-금융-AI-Challenge/PLAN.md`와 `RALPH-PROMPT.md`를 기준으로, 캘린더 기반 기회·지원 에이전트의 검증 가능한 웹 MVP 1차 완성본을 구현한다. 프로젝트명 확정 전까지 저장소 생성·커밋·푸시는 하지 않는다. 필수 면접/시험/증빙 복구/정책 상태 시나리오, 모바일·데스크톱 검증, README와 기능명세서, architect 최종 승인을 모두 완료해야 한다.
```
